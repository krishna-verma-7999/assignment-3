import { Box } from "@mui/material";

const Home = () => {
  return (
    <Box
      sx={{
        display: "flex",
        flexDirection: "column",
        margin: "auto",
        gap: "10px",
        paddingTop: "15px",
      }}
    >
      <div>Helloˍ</div>
      <div>Helloˍ</div>
    </Box>
  );
};

export default Home;
